//----------------------------------------------------------------------------------------------------
// mainMenuComponent
//----------------------------------------------------------------------------------------------------
export const mainMenuComponent = `

MainMenuComponent is a React Component.  

There is only one instance of the MainMenuComponent.

It is a row of buttons that spans the width of the browser window.

It has two left-aligned buttons: "Demo" and "Workbooks"

It has one right-aligned button: "Sign Out"

The "Demo" button, when clicked, adds a DemoFrame to the canvas.

The "Workbooks" button, When clicked, adds a WorkbookListFrame to the canvas.

The "Sign Out" button, When clicked, does a Cognito signout.
`
